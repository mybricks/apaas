import page from "./UserManagementPage";
import header from "./UserManagementHeader";
import { id, UserManagementMenuButton as button } from "./UserManagementMenuButton";

export { button };

export default {
  id,
  page,
  header,
};
